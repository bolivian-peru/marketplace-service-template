{
  "name": "x-intelligence-search",
  "version": "1.0.0",
  "description": "X/Twitter Real-Time Search API powered by Proxies.sx mobile proxies with x402 payment gating",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
